import { makeStyles } from "@material-ui/core/styles";

const useStyles = makeStyles({
  gridContainer: {
    paddingLeft: "20px",
    paddingRigh: "20px",
  },
  cardSection: {
    marginBottom: "25px"
  },
  cardHeaderTitle: {
    textAlign: "center",
  },
});

export default useStyles;
